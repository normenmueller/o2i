# Handoff

- Observed: 2026-08-26 CEST
- Work status: `ACTIVE`
- Current Issue: `NONE`
- Current node: `accepted-awaiting-commit`

# Objective

Complete routine O2I cold-start transitions with one safe, exact three-step Product Owner instruction while keeping normal startup state-free and repository-owned.

# Authority

- The Product Owner explicitly authorized this Issue-free Routine maintenance for routine cold-start completion instructions.
- Scope is limited to `.ai4x/BEHAVIOR.md`, `.ai4x/STATE.md`, and `utl/governance/test_github_governance.py`; no product semantics, model, Haskell product code, remote mutation, or publication is authorized outside this exact bounded continuity candidate.
- The Product Owner explicitly requested durable future recording, and the primary Gertrud announced separate repository-native publication; this authorizes the local commit, branch publication, merge, and clean `trunk` reconciliation for this exact bounded candidate.
- The independent #52 branch and its repository-local return point remain separate, preserved, and untouched; this maintenance neither activates nor changes them.

# Material Risk

- `/delete` permanently removes the completed current session and descendants, so it must never be recommended before the durable return point, completed work and review, and no-live-work preconditions hold or when transcript retention is required.
- The three routine steps must remain actionable, use the Product Owner's language, derive the repository root without an absolute host path, and carry no path, digest, snapshot, or handoff payload.
- An external Repository Autonomy, Agentic Workflow, and Human Usability Co-Author owns design and implementation of this candidate and did not accept it.

# Verification

- Basis: accepted `trunk` revision `b9b86b1220c39cb2c433e58201253a47d98b4c9e`.
- This target worktree was clean before implementation.
- Candidate scope is the three files named under Authority; product artifacts and the independent #52 worktree remain outside it.
- Governance 30, Verification Scope 18, Licensing 10, REUSE 494/494, package-license, and `git diff --check` checks pass.
- An independent reviewer accepted the exact candidate at binary diff SHA-256 `ea75d2c2d31162e49edfefc23010c9ab4dd056f9fdf8549a261c7fdd0ff79ca3` with no blocking or advisory finding; the reviewer did not author or implement the candidate.

# Next Action

Create the authorized local commit, then perform separate repository-native branch publication and merge, and reconcile a clean `trunk` without touching #52.

# Local Return Point

- Branch: `chore/cold-start-po-instructions`.
- Basis: `b9b86b1220c39cb2c433e58201253a47d98b4c9e`.
- Dirty scope: `.ai4x/BEHAVIOR.md`, `.ai4x/STATE.md`, and `utl/governance/test_github_governance.py` only.
- Resume this Routine maintenance only; preserve #52 exactly as an independent return point.
