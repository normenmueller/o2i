# Valid AMX Reference Fixtures

`native/minimal.archimate` was genuinely saved by the user with Archi 5.9.0.
Its exact SHA-256 and native identities are admitted in `manifest.json` and
`catalog.json`. It is intentionally decode-only: the file has no O2I profile
and its auto-created Default View is not an inspectable O2I scope.

`orientation/ethos.archimate` was also genuinely saved by the user with Archi
5.9.0. Its admitted View projects exactly one Ethos contextualizing one
Principle and passes Profile, Structure, and Semantics without requiring a
context macrorelation.

The remaining seven required paths and their cumulative semantic contracts are
fixed in `catalog.json`; each remains `pending-external-archi-save` without an
unverified integrity claim.

The native reference is a focused adapter fixture and proves Decode only. The
orientation and later references are semantic models passed through Inspection.
The orientation sequence is cumulative: Ethos; Ethos plus Mission; Ethos plus
Mission plus Vision. The later sequence extends that orientation baseline with
one localized contract for Strategy formation, situated Need, Intervention,
Measure, and finally one complete effect trace.

A further file is admitted only after a real Archi 5.9.0 save. Its manifest
entry must record the exact SHA-256, native version, direct root profile where
applicable, stable selected View ID and name, and expected projected node, edge,
occurrence, and provenance identities. Strategy-bearing references also bind a
complete test `RawStrategyFormulation` to manifested stable IDs; AMX itself does
not persist that supplemental semantic input.

`mdl/o2i.archimate` remains a repository integration input and is not copied or
reclassified as a unit fixture.
